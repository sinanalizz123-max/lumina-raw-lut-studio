# Build outputs / APKs (never commit binaries)
*.apk
*.aab
*.ap_
# Gradle / build outputs
.gradle/
build/
app/build/
.kotlin/

# Local machine config (SDK path, secrets) — never commit
local.properties

# Android Studio
.idea/
*.iml
captures/
.externalNativeBuild/
*.hprof

# OS
.DS_Store
Thumbs.db
