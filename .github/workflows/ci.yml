name: Lumina CI

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Accept licenses and install platform
        run: |
          yes | sdkmanager --licenses > /dev/null
          sdkmanager "platforms;android-35" "build-tools;36.0.0"

      - name: Assemble debug
        run: chmod +x gradlew && ./gradlew :app:assembleDebug --console=plain --max-workers=4

      - name: Unit tests
        run: ./gradlew :app:testDebugUnitTest --console=plain --max-workers=4

      - name: Lint
        run: ./gradlew :app:lintDebug --console=plain --max-workers=4

      - name: Upload debug APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: lumina-debug-apk
          path: app/build/outputs/apk/debug/app-debug.apk
